# Welcome to React Router!

## Setup

Before running the project make sure you have `credentials.json` file in the root of the project and run the following commands:

```bash
export GOOGLE_APPLICATION_CREDENTIALS=credentials.json
npm run dev
```
